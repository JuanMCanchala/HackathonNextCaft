# Domain model

The core model is workspace-first: memberships establish access, detections preserve evidence immutably, incidents represent explicit state transitions, and audit records explain security-relevant changes. Validate intent against `docs/SENTRA-PRD.md`, `docs/DOMAIN-DESIGN.md`, and `docs/USE-CASES.md`; current code/tests win for shipped behavior.
