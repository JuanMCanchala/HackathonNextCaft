# Critical constraints

Use `identity.tokenIdentifier`, workspace-first authorization, validators, indexes, pagination, bounded reads, action runtime boundaries, immutable detections, idempotency, auditability, DTO schemas, and secret hygiene. Required references: `RULES.md`, `docs/BACKEND-TEST-CASES.md`, `docs/API-CONTRACT.md`, `docs/api-contract.schemas.json`, `openspec/config.yaml`.
