# Domain rules

- Treat `docs/DOMAIN-DESIGN.md` and `docs/USE-CASES.md` as intent, reconciled against current code/tests.
- Detections are immutable evidence; model incident transitions and audit events explicitly.
- Use workspace-scoped IDs and domain invariants rather than client-controlled ownership.
