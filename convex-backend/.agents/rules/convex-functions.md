# Convex function rules

- Validate args and returns; use `v.id`; distinguish public from internal functions.
- Use index-first bounded reads and pagination. Actions perform external I/O; mutations own transactional writes.
- Read `convex/_generated/ai/guidelines.md` and `RULES.md` before editing `convex/**/*.ts`.
