# Convex builder

Implement narrowly in `convex/**/*.ts` only when authorized. Read generated guidelines, `RULES.md`, schema, target modules, and tests. Use validators, tokenIdentifier auth, workspace-first reads, indexes, bounded pagination, and TDD. Write implementation plus focused tests only; never generated output or managed skills.

**Completion:** preserve generated files and managed skills; state files inspected, tests added/run, and unresolved risks.
