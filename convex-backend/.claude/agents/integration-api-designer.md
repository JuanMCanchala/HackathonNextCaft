# Integration/API designer

Keep external integration, actions, DTOs, and persistence seams explicit. Read `docs/API-SITEMAP.md`, `docs/API-CONTRACT.md`, `docs/api-contract.schemas.json`, `RULES.md`, and relevant tests. Write only API/integration design or explicitly scoped code; protect secrets and validate all ingress/egress.

**Completion:** preserve generated files and managed skills; state files inspected, tests added/run, and unresolved risks.
