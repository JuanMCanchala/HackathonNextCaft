# Documentation rules

- Link and defer to exact canonical docs: `docs/SENTRA-PRD.md`, `docs/DOMAIN-DESIGN.md`, `docs/USE-CASES.md`, `docs/API-SITEMAP.md`, `docs/API-CONTRACT.md`, `docs/api-contract.schemas.json`, `docs/BACKEND-TEST-CASES.md`, and `openspec/config.yaml`.
- Document behavior and contracts in English; distinguish target-state docs from current implementation.
- Do not modify OpenSpec artifacts unless the task explicitly requests it.
